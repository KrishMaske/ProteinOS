import { describe, expect, it } from 'vitest'; import { calculateWorkoutVolume, countWorkingSets, estimatedOneRepMax } from './workout-math';
describe('workout math', () => { const sets = [{ weightKg: 100, reps: 5, completed: true, type: 'working' as const }, { weightKg: 60, reps: 8, completed: false, type: 'working' as const }, { weightKg: 40, reps: 10, completed: true, type: 'warmup' as const }]; it('counts only completed load volume', () => expect(calculateWorkoutVolume(sets)).toBe(900)); it('excludes warmups from working count', () => expect(countWorkingSets(sets)).toBe(1)); it('calculates estimated one rep max', () => expect(estimatedOneRepMax(100, 5)).toBeCloseTo(116.67, 1)); });
